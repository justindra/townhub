{
  "name": "@townhub/test",
  "private": true,
  "version": "0.0.0",
  "main": "dist/index",
  "license": "MIT",
  "devDependencies": {},
  "dependencies": {
    "@townhub-libs/core": "^0.0.0"
  },
  "scripts": {
    "prebuild": "rm -rf dist && rm -f tsconfig.build.tsbuildinfo",
    "build": "tsc -b tsconfig.build.json",
    "test": "tsc -p tsconfig.json"
  }
}
